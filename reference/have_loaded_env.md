# Check if there is a loaded `conda` environment.

Function to check if a `conda` environment is already loaded.

## Usage

``` r
have_loaded_env()
```

## Value

Boolean, `TRUE` if an environment is loaded, `FALSE` otherwise.
