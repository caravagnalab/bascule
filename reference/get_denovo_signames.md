# Get denovo signatures names

Get denovo signatures names

## Usage

``` r
get_denovo_signames(x, types = get_types(x))
```

## Arguments

- x:

  bascule object.

- types:

  List of variant types to retrieve signames for.

## Value

list of de novo signature names with names equal to \`types\`.
