# Retrieve the name of the currently loaded environment.

Function to retrieve the name of the currently loaded `conda`
environment.

## Usage

``` r
which_conda_env()
```

## Value

a `character` corresponding to the name of the loaded environment.
