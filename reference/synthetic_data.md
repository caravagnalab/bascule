# Analysis of a synthetic cohort

Input SBS and DBS counts and BASCULE fits for a cohort of 150 samples
whose counts and exposures have been generated from the BASCULE
generative model.

## Usage

``` r
data(synthetic_data)
```

## Format

An object of class `list` of length 3.
