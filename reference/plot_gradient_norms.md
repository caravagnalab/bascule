# Plot parameters gradients norms

Visualise the norms of the parameters gradients computed during the
inference.

## Usage

``` r
plot_gradient_norms(x, types = get_types(x))
```

## Arguments

- x:

  bascule object.

- types:

  List of types to visualise gradient norms for.

## Value

patchwork object
